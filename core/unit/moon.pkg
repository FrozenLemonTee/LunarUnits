// Unit definition and conversion package: depends on the algebra and
// dimension layers.
import {
  "FrozenLemonTee/LunarUnits/core/algebra",
  "FrozenLemonTee/LunarUnits/core/dimension",
}

// Doctests construct dimensions directly, so the dimension package must also be
// importable from black-box tests.
import {
  "FrozenLemonTee/LunarUnits/core/dimension",
} for "test"
