# Constants

The `constants` area contains reusable quantity values for common scientific
constants.

- `physics`: speed of light, standard gravity, gravitational constant, Planck
  constant, Avogadro constant and Boltzmann constant.

Constants are ordinary `Quantity` values, so they compose with the same unit
algebra and dimension checks as user-created quantities.
